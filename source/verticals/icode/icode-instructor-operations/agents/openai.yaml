interface:
  display_name: "iCode Instructor Operations"
  short_description: "Handle instructor lifecycle through BOS"
  default_prompt: "Use $icode-instructor-operations to handle an instructor hiring, onboarding, teaching, or offboarding request through BOS."
dependencies:
  tools:
    - type: "mcp"
      value: "bos_icode"
      description: "BOS remote agent gateway for iCode organization"
      transport: "streamable_http"
      url: "https://dfsm.ai/mcp"
policy:
  allow_implicit_invocation: true
