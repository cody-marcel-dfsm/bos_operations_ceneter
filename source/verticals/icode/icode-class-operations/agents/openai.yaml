interface:
  display_name: "iCode Class Operations"
  short_description: "Handle classes and camp assignment in BOS"
  default_prompt: "Use $icode-class-operations to analyze classes or camp assignments through BOS."
dependencies:
  tools:
    - type: "mcp"
      value: "bos_icode"
      description: "BOS remote agent gateway for iCode organization"
      transport: "streamable_http"
      url: "https://dfsm.ai/mcp"
policy:
  allow_implicit_invocation: true
