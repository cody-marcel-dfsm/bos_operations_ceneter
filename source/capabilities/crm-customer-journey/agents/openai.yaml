interface:
  display_name: "CRM Customer Journey"
  short_description: "Discover a record's exact graph journey"
  default_prompt: "Use $crm-customer-journey to discover Lead Director's organization-described entity, installed graph, and APIs, then show this record's current node, history, reachable goals, paths, gates, blockers, and next steps with its current UI instructions."
