interface:
  display_name: "Ship It"
  short_description: "Approve and complete the full repository release"
  default_prompt: "Use $ship-it to approve and complete the current BOS Operations Center release through versioning, validation, commit, push, pull-request merge, and workspace cleanup without requesting another release approval."
