# Recent-meeting follow-up conformance walkthrough

This is a fixture path, not a provider-routing rule. Resolve every current
operation, structured source, method, URI, schema, limit, and action through
authenticated discovery.

1. Describe the Calendar search and read operations required by the prompt.
2. Search the bounded recently-ended window using the exact returned source and
   choose the event with the latest completed end time.
3. Invoke that event's exact read action with `{}` only when its discovered
   closed empty-object schema requires it. Use its declared attendee fields;
   do not search CRM for those attendees.
4. Read fresh BOSL resources, plugin journeys, and detailed service contracts.
   Author the complete raw graph with actual seed data, a searchable category,
   server-owned audience materialization, automation-template resolution,
   campaign preparation, client review, sanctioned send, result display,
   bounded repair, and terminal outcomes.
   `email_list.materialize` must appear as an enabled, ready, discovered
   semantic operation with its complete published contract. When it is absent
   or unavailable, stop before registration and report that exact server
   release dependency. Never substitute a provider, plugin source, or generic
   storage operation.
5. Register raw BOSL, invoke the bodyless start, and display the exact sanitized
   category, recipient count, subject, and complete emitted message returned by
   the review instruction.
6. After exact approval, invoke only the returned `complete`, `step`, and state
   actions. BOS performs the send. Display the sanitized send result, complete
   the result step, invoke its successor, and report terminal evidence.

The controlled executable acceptance fixture has exactly one approved recipient.
Automated tests use a deterministic non-sending provider sandbox. A live send
is a separate owner-gated acceptance action. The client receives no attendee
list, provider identifier, campaign identifier, audience identifier, artifact
reference, or server execution identifier from journey lifecycle responses.

For later campaign status or metrics questions, discover those query contracts
again and send the category only. Include a complete observation window only
when the user supplied valid bounds. Present the server-returned effective
window, observation time, state or metrics, and sanitized issues. Available
semantic actions require fresh discovery and are never journey transitions.
