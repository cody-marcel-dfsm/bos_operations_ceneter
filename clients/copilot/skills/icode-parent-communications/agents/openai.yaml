interface:
  display_name: "iCode Parent Communications"
  short_description: "Handle parent calls, texts, and reviews"
  default_prompt: "Use $icode-parent-communications to handle parent calls, texts, or reviews through BOS."
dependencies:
  tools:
    - type: "mcp"
      value: "bos_icode"
      description: "BOS remote agent gateway for iCode organization"
      transport: "streamable_http"
      url: "https://dfsm.ai/mcp"
policy:
  allow_implicit_invocation: true
