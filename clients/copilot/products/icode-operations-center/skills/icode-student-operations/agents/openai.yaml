interface:
  display_name: "iCode Student Operations"
  short_description: "Reconcile BOS and Care.com enrollments"
  default_prompt: "Use $icode-student-operations to handle an enrollment, Care.com confirmation, or student progress request."
dependencies:
  tools:
    - type: "mcp"
      value: "bos_icode"
      description: "BOS remote agent gateway for iCode organization"
      transport: "streamable_http"
      url: "https://dfsm.ai/mcp/apps/leaddirector/icode-operations"
policy:
  allow_implicit_invocation: true
