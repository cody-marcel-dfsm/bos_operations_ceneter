interface:
  display_name: "iCode Class Operations"
  short_description: "Handle classes, camps, and Care.com enrollments"
  default_prompt: "Use $icode-class-operations to generate next week's five-day camp roster image with each student and camp repeated by attendance day, plus one primary phone entry per family."
dependencies:
  tools:
    - type: "mcp"
      value: "bos_icode"
      description: "BOS remote agent gateway for iCode organization"
      transport: "streamable_http"
      url: "https://dfsm.ai/mcp/apps/leaddirector/icode-operations"
policy:
  allow_implicit_invocation: true
