# Release 0.4.61 review

Date: 2026-08-31

## Scope

Reviewed the complete versioned release diff for `0.4.61`, including the
readable plugin-console and plugin-settings value contract, canonical package
metadata, generated Codex/Claude/Copilot/Gemini clients, tests, Vault sources,
and release documentation.

## Findings

No material findings remain.

## Evidence

- Canonical package and both active product manifests declare `0.4.61`, and the
  customer installation guide identifies `0.4.61` as the current desktop
  marketplace release (`package.json:3`, `products/bos/product.json:4`,
  `products/education-center/product.json:4`, `README.md:31`).
- The Plugin Console requires actual readable row values, expands display-safe
  properties, linkifies safe contact values, and uses real host controls only
  when a native component is mounted
  (`source/platform/bos-plugin-console/SKILL.md:57-86`).
- Typed settings reject the generic tool-result viewer as completed rendering,
  require actual values, and define readable type-aware presentation for nested
  values, links, empty values, schedules, and authorized fallback actions
  (`source/platform/bos-plugin-settings/SKILL.md:69-97`).
- The architecture, console specification, and settings design preserve
  server-owned state, one root BOS connection, opaque selectors, remote MCP App
  ownership, and an immediate conversational fallback
  (`Vault/docs/architecture.md:280-318`,
  `Vault/specs/plugin-service-console.md:157-172`,
  `Vault/docs/plugin-settings-streaming-sync-design.md:456-499`).
- Focused regressions require visible values, clickable safe links, expanded
  nested data, and real-control semantics
  (`tests/plugin-console.test.mjs:19-62`,
  `tests/plugin-settings.test.mjs:45-82`).
- `npm run release:check` regenerated both active products for Codex, Claude,
  Copilot, and Gemini; package structure, product composition, credential scan,
  and the single-BOS connection contract passed; all 195 tests passed.
- Canonical and generated console/settings skill bytes match across every
  supported client, and `git diff --check` passed.

## Publication boundary

The merged release pull request makes `0.4.61` available on the tracked Git
marketplace ref and supplies the Claude organization synchronization event when
automatic marketplace sync is enabled. Independent Claude account refresh,
official Anthropic publication, private Codex marketplace refresh, and public
ChatGPT/Codex directory publication remain host-owned actions.

APPROVED
