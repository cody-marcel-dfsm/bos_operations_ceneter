# BOS Operations Center 0.4.64 release review

Date: 2026-09-01

## Scope

Review of the complete release diff for native Codex authentication discovery,
the Operations Center-to-server execution boundary, server handoff acceptance
requirements, generated client parity, and the 0.4.64 release metadata.

## Findings

No material findings remain.

- `AGENTS.md:18-35` fixes execution authority to this repository and requires
  one continuous server handoff prompt containing the complete client-owned
  acceptance suite.
- `source/platform/implementation/SKILL.md:8-31` prevents implementation work
  from crossing into a server repository, worktree, infrastructure, database,
  or deployment while preserving a contract-complete server handoff.
- `source/platform/bos-mcp-client/SKILL.md:22-40` applies the same ownership
  boundary to MCP recovery and server-runtime requirements.
- `Vault/specs/single-bos-mcp-connection.md:36-55` makes HTTP 401 protected-
  resource discovery and the native **Authenticate** action the canonical
  signed-out lifecycle and removes the retired CLI workaround.
- `Vault/specs/single-bos-mcp-connection.md:129-162` requires the client-owned
  structural, signed-out discovery, and OAuth authorization probes for every
  server release affecting MCP authentication.
- `scripts/lib/bos-oauth-live-contract.mjs:3-90` validates the exact deployed
  BOS resource, its matching protected-resource metadata challenge, HTTP 401,
  and structured `authentication_required` response.
- `tests/bos-oauth-live-contract.test.mjs:16-92` covers canonical success,
  HTTP 405 failure, challenge mismatch, and candidate-resource reporting.
- `tests/package-model.test.mjs:602-656` enforces the repository boundary,
  single-prompt handoff, native authentication contract, and complete server
  acceptance suite against real canonical repository files.

## Validation

- All three changed skills passed the skill validator.
- `npm run release:check` regenerated two active products for Codex, Claude,
  Copilot, and Gemini; package structure and credential scanning passed; the
  single-BOS-connection contract passed; and all 203 tests passed.
- `git diff --check` passed.
- Live discovery and authorization probes remain server-release acceptance
  commands and were not invoked without deployed-candidate inputs.
- No server repository, server branch, infrastructure, or deployment was
  changed by this release workflow.

APPROVED
