# Oracle review: release 0.4.71 Codex Login regression

Date: 2026-09-01

## Scope

Review the complete release diff from `origin/main` through
`codex/release-v0.4.71`, including the exact registered-app restoration,
canonical package model, generated clients, bounded installation migration,
runtime verification, regression tests, release metadata, corrected incident
history, and all new Vault manifests. BOS server code and infrastructure remain
outside the repository and outside this diff.

## Findings

No blocking or advisory findings.

## Evidence

- `products/bos/product.json:46-50` assigns the root runtime the exact
  user-proven app identity from working commit `e46546c`.
  `clients/codex/plugins/bos/.codex-plugin/plugin.json:29-30` points to
  `.app.json`; `clients/codex/plugins/bos/.app.json:1-8` declares exactly one
  required BOS app. The prior direct `.mcp.json` is removed.
- `scripts/lib/package-model.mjs:214-252` reserves registered-app ownership for
  the root Codex runtime and rejects it on subservices. Lines 524-540 generate
  the package-owned app binding deterministically.
- `scripts/install-package.mjs:129-167` rejects absent, optional, malformed,
  mismatched, credential-bearing, or direct-MCP-shadowed root bindings. Lines
  645-700 migrate the known direct-MCP regression and replacement app IDs while
  preserving unrelated client state.
- `scripts/verify-codex-runtime.mjs:74-106` independently verifies the installed
  registered-app binding. Lines 163-221 keep that result distinct from the
  callable-tool catalog result.
- `tests/codex-login-surface-contract.test.mjs:7-64` pins the exact working app
  identity, `required: true`, absence of `.mcp.json`, and independence of the
  display contract from server OAuth discovery. Installer and runtime tests
  cover both regressed upgrade shapes and reject missing, optional, mismatched,
  and shadow bindings.
- `contracts/single-bos-mcp-connection.v1.json` and
  `Vault/specs/single-bos-mcp-connection.md:17-45` preserve one immutable BOS
  resource while requiring the native Codex Login display binding separately
  from OAuth activation and callable discovery.
- `Vault/docs/issues/ISSUE_HISTORY.md:9-81`, its linked conclusion, and
  `Vault/docs/codex-registered-app-incident.md:1-111` record the successful
  Education-to-BOS conversion, the 0.4.55-through-0.4.70 regression chain, the
  invalidated fixes, the 0.4.71 correction, remaining host verification, and
  prevention controls.
- `.agents/skills/oracle/SKILL.md:26-66` requires future reviewers to assess
  registered-app display, OAuth activation, grant state, and callable discovery
  as independent layers. The Oracle remains absent from customer packages.
- `package.json:3`, both active product manifests, and all generated package
  metadata identify release 0.4.71. Deterministic regeneration changed no
  connection owner and introduced no server-side file.

## Validation

- `npm run release:check`: passed for 0.4.71; package generation, package and
  credential validation, the single-BOS contract, and all 214 tests passed.
- `npm run contract:check`: passed with zero violations as part of the release
  gate.
- `git diff --check`: passed.
- Vault sync and Chroma query returned the corrected incident, accepted
  registered-app decision, invalidated direct-MCP review, and current release
  history.
- Live server OAuth discovery was read-only and healthy before this release;
  no server repository or infrastructure was changed. Visual Login display and
  signed-in execution remain explicit post-install host acceptance checks.

## Conclusion

Release 0.4.71 restores the exact registered BOS app that previously displayed
Login, removes the direct-MCP display regression, migrates affected client
packages, and adds durable tests that prevent transport receipt from satisfying
the independent Login display contract. The release preserves one BOS
connection, transport-free subservices, credential containment, and the
Operations Center repository boundary.

APPROVED
