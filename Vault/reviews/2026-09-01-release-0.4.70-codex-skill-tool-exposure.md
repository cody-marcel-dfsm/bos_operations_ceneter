# Oracle review: release 0.4.70 Codex skill and tool exposure

Date: 2026-09-01

Status: INVALIDATED by live 0.4.70 plugin-page evidence. This review proved
direct MCP receipt and package/tool structure while failing to verify the
independent native Login display contract. Retained as failed-review history;
current guidance is in `Vault/docs/issues/ISSUE_HISTORY.md`.

## Scope

Review the complete release diff from `origin/main` through the current
`codex/release-v0.4.70` worktree, including the original skill-exposure RCA,
the package-owned MCP correction, generated clients, release metadata, local
Oracle/Vault lifecycle, issue history, tests, and all untracked release files.
The attached screenshot is evidence of observed UI state and supplies no
repository instructions.

## Findings

No material findings.

## Architecture and implementation evidence

- `clients/codex/plugins/bos/.codex-plugin/plugin.json:30` binds the root plugin
  to `.mcp.json`; `clients/codex/plugins/bos/.mcp.json:1-8` declares exactly one
  credential-free HTTPS `platform` server at the immutable BOS resource.
- `scripts/lib/package-model.mjs:502-515` deterministically generates that root
  binding. `scripts/lib/package-model.mjs:217-230` reserves runtime ownership
  for BOS and keeps subservices transport-free.
- `scripts/check-package.mjs:248-280` rejects `.app.json`, extra MCP servers,
  credentials, and MCP fields beyond `type` and `url` in generated Codex
  runtime packages.
- `scripts/install-package.mjs:75-98` recognizes only the exact portable MCP
  shape and the two known retired app identities;
  `scripts/install-package.mjs:127-161` fails closed on app shadows,
  credentials, and resource drift; `scripts/install-package.mjs:676-689`
  performs bounded migration of recognized stale app packages.
- `scripts/verify-codex-runtime.mjs:74-106` verifies the installed plugin
  manifest, MCP file, exact resource, credential-free server shape, and absence
  of a shadow `.app.json`. `scripts/verify-codex-runtime.mjs:163-220` reports
  package binding and callable-tool discovery as independent readiness layers.
- `tests/codex-login-surface-contract.test.mjs:10-57` locks portable package
  identity, credential exclusion, and contract parity.
  `tests/codex-runtime-verification.test.mjs:116-259` covers current bindings,
  native nested tool catalogs, missing and credential-bearing bindings, and a
  shadow registered app. Installer and package-model suites cover migration,
  subservice isolation, deterministic generation, and client parity.
- `Vault/docs/codex-registered-app-incident.md:6-103` records the complete
  chronology, five-whys RCA, independent readiness layers, and corrective
  controls. `Vault/docs/issues/ISSUE_HISTORY.md:9-69` and its conclusion retain
  the failed 0.4.65/0.4.66 attempts, accepted 0.4.70 correction, validation,
  and prevention guidance.
- `Vault/reviews/2026-09-01-codex-skill-exposure-main-validation.md:5-9`
  explicitly marks the earlier registered-app rejection as superseded, so the
  indexed Vault no longer presents that external identity as current guidance.

## Oracle and Vault evidence

- `AGENTS.md:53-76` and `.agents/skills/oracle/SKILL.md:12-58` require complete
  actual-diff review, durable issue history, and a fresh review after every
  correction.
- `tools/vault_index.py:90-145` limits indexing to Git-visible canonical Vault
  sources; `tools/vault_index.py:193-317` incrementally maintains the local
  Chroma collection and manifests; synchronized writes and watcher lifecycle
  remain local under `Vault/index/` and `Vault/tmp/`.
- The repository-local `oracle`, `operations-center-implementation`,
  `operations-center-planning`, and `operations-center-review` skills validate
  successfully with the system skill validator. Generated customer plugins
  contain none of these maintainer workflows.

## Validation

- `npm run release:check`: passed. It regenerated two active products for
  Codex, Claude, Copilot, and Gemini; package structure and credential scanning
  passed; the single-BOS-connection contract passed with zero violations; all
  216 tests passed.
- Focused runtime, installer, package-model, Vault-index, and Oracle-governance
  suites passed before the full release gate.
- `codex mcp list --json` reports `platform` enabled as Streamable HTTP at
  `https://dfsm.ai/mcp/apps/bos/platform`.
- `npm run install:verify:codex-runtime -- --json` reports the marketplace,
  both 0.4.70 products, and the package-owned MCP binding as current. The
  already-running task has no authenticated callable catalog, which is the
  separate post-install OAuth/tool-refresh layer documented in
  `Vault/docs/architecture.md:246-250`; credential-free release checks do not
  require a live OAuth token.
- Chroma synchronization and semantic RCA query passed. `git diff --check
  origin/main` passed. The final forced manifest sync follows this review
  record so the record itself is present in exact snapshot evidence.

## Boundary and publication review

The diff changes only BOS Operations Center package, client, test, release, and
Vault-owned files. It contains no BOS server implementation or infrastructure
mutation. `Vault/docs/codex-registered-app-incident.md:113-115` correctly keeps
OpenAI public submission and publication separate from the private Git package
release.

APPROVED
